{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "drag-permission",
  "description": "Allows the main window to be dragged",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}